### 备忘录环境变量
- SMTP_HOST: 邮箱smtp服务器
- SMTP_PORT: smtp端口(465?)
- FROM_EMAIL: 发送者邮箱地址
- FROM_PASSWORD: 邮箱授权码
- TO_EMAIL: 目的邮箱，可设置多个，使用","分割
- SSL_ENABLE: 使用http/https，值为1开，其余视为不开. 证书文件名称应为ser.pem(证书)与ser.key(私钥)
- OAUTH_SECRET: OAuth的应用密钥
